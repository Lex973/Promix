<?php
/**
 * Template Name: Главная PROMIX
 *
 * Шаблон нужен не ради вывода — главную рисует front-page.php, — а чтобы
 * к нему привязать поля секций. Привязка к ID главной ломается, стоит
 * поменять главную в «Настройках → Чтение» или перенести сайт: поля
 * молча исчезают из админки, а сайт показывает значения по умолчанию.
 *
 * @package promix
 */

defined( 'ABSPATH' ) || exit;

require get_theme_file_path( 'front-page.php' );
